# Archived examples

These scenarios target the retired HTTP/WebSocket and `lp://` overlay. They are
kept only as migration fixtures and protocol history; they are not supported
runtime examples and no package script executes them. Use the LNP/1 enrollment,
Gateway and client flows in the repository root `README.md` and `RUNBOOK.md`.
